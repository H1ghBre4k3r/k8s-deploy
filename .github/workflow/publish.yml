name: publish

on: [push]

jobs:
    publish-hello-world-image:
        runs-on: ubuntu-latest

        steps:
            - uses: actions/checkout@v2

            - name: Build the hello-world Docker image
              run: |
                  echo $GH_PAT | docker login ghcr.io -u H1ghBre4k3r --password-stdin
              env:
                  GH_PAT: ${{ secrets.GH_PAT }}
